import React from "react";
import Navigation from "./Navigation";

function Header (){

    return(
        <div>
        <p>header test</p>
        <Navigation/>
        </div>
    )
};

export default Header;