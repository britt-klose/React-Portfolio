import React from "react";
import Navigation from "../components/Navigation";

function Header (){

    return(
        <div>
        <p>header test</p>
        <Navigation/>
        </div>
    )
};

export default Header;